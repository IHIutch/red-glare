---
"@red-glare/create": patch
---

Generated projects now depend on the current @red-glare/astro release; project names are validated and escaped; the scaffolder refuses to overwrite non-empty directories, emits a .gitignore, and handles --help.
